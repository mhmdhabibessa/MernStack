// parent Ninja class
class Ninja {
    constructor(health,speed,strength ,name) {
        this.name = name;
        this.health = health;
        this.speed = 3;
        this.strength = 3;
    }
    sayName() {
        this.miles += 10;
        console.log(`${ this.constructor.name } `);
    }

   showStats(){	
console.log(`${ this.constructor.name } `);
console.log(`${ this.constructor.health} `);
console.log(`${ this.constructor.speed } `);
console.log(`${ this.constructor.strength } `); 
	
}
drinkSake(){
	this.speed+=10;
	
}

const ninja1 = new Ninja("Hyabusa");
ninja1.sayName();
ninja1.showStats();
}

class sensei extends Ninja{
    constructor(health,speed,strength ,name,widsom) {
	super("200", "10","10", name);
	this.wisdom=10 ;
         }
    // simple function in the child class
    speakWisdom() {
        // by using super, we can call the parent method
        const message = super.drinkSake();
        console.log(message);
    }

